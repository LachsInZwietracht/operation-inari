import { DashboardSkeleton } from "@/components/page-skeletons"

/** Route-level skeleton while the server resolves practice statistics. */
export default function PraxisStatistikenLoading() {
  return <DashboardSkeleton />
}
