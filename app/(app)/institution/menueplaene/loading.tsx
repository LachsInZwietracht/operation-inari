import { DetailPageSkeleton } from "@/components/page-skeletons"

/** Route-level skeleton while the server resolves menu cycles. */
export default function MenueplaeneLoading() {
  return <DetailPageSkeleton />
}
