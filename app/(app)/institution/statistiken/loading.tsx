import { DashboardSkeleton } from "@/components/page-skeletons"

/** Route-level skeleton while the server resolves institution statistics. */
export default function StatistikenLoading() {
  return <DashboardSkeleton />
}
