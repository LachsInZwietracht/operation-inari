import { DetailPageSkeleton } from "@/components/page-skeletons"

/** Route-level skeleton while the server resolves a recipe. */
export default function RezeptDetailLoading() {
  return <DetailPageSkeleton />
}
