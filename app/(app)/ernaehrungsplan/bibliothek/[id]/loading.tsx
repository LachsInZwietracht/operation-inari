import { DetailPageSkeleton } from "@/components/page-skeletons"

/** Route-level skeleton while the server resolves a library plan. */
export default function BibliothekDetailLoading() {
  return <DetailPageSkeleton />
}
